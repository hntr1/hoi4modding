GER_bop_decisions_cat = {
	GER_bop_take_wehrmacht_advice = {
		visible = {
			always = yes
		}

		available = {
			always = yes
		}

		cost = 10

		complete_effect = {
			add_power_balance_value = { # Progress balance of power towards the Wehrmacht
				id = GER_balance_of_power
				value = 0.1
			}
		}

		days_remove = 30 # 30 day cooldown
	}

	GER_bop_take_ss_advice = {
		visible = {
			always = yes
		}

		available = {
			always = yes
		}

		cost = 10

		complete_effect = {
			add_power_balance_value = { # Progress balance of power towards the SS
				id = GER_balance_of_power
				value = -0.1
			}
		}

		days_remove = 30 # 30 day cooldown
	}

	GER_bop_appoint_himmler_command = {
		visible = {
			NOT = { # Check that Himmler isn't already commander 
				has_idea = GER_bop_himmler_command
			}
		}

		available = {
			always = yes
		}

		custom_cost_trigger = { # Costs 15 Command Power
			NOT = {
				command_power < 15
			}
		}
		custom_cost_text = decision_cost_CP_15

		complete_effect = {
			hidden_effect = { # Deduct cost
				add_command_power = -15
			}
			add_ideas = GER_bop_himmler_command # Add pro SS national spirit
			add_power_balance_value = { # Progress balance of power towards the SS
				id = GER_balance_of_power
				value = -0.05
			}
		}
	}

	GER_bop_remove_himmler_command = {
		visible = { # Check that Himmler is commander
			has_idea = GER_bop_himmler_command
		}

		available = { # Check that Himmler is commander
			has_idea = GER_bop_himmler_command
		}

		custom_cost_trigger = { # Costs 15 Command Power
			NOT = {
				command_power < 15
			}
		}
		custom_cost_text = decision_cost_CP_15

		complete_effect = {
			remove_ideas = GER_bop_himmler_command # Remove pro SS national spirit
			hidden_effect = { # Deduct cost
				add_command_power = -15
			}
			add_power_balance_value = { # Reverse balance of power effect
				id = GER_balance_of_power
				value = 0.05
			}
		}
	}

	GER_bop_appoint_goring_command = {
		visible = {
			NOT = { # Check that Goring isn't in command
				has_idea = GER_bop_goring_command
			}
		}

		available = {
			always = yes
		}

		custom_cost_trigger = { # Costs 15 Command Power
			NOT = {
				command_power < 15
			}
		}
		custom_cost_text = decision_cost_CP_15

		complete_effect = {
			hidden_effect = { # Deduct cost
				add_command_power = -15
			}
			add_ideas = GER_bop_goring_command # Add pro Wehrmacht national spirit
			add_power_balance_value = { # Progress balance of power towards the Wehrmacht
				id = GER_balance_of_power
				value = 0.05
			}
		}
	}

	GER_bop_remove_goring_command = {
		visible = { # Check that Goring is in command
			has_idea = GER_bop_goring_command
		}

		available = { # Check that Goring is in command
			has_idea = GER_bop_goring_command
		}

		custom_cost_trigger = { # Costs 15 Command Power
			NOT = {
				command_power < 15
			}
		}
		custom_cost_text = decision_cost_CP_15

		complete_effect = {
			remove_ideas = GER_bop_goring_command # Remove pro Wehrmacht national spirit
			hidden_effect = { # Deduct cost
				add_command_power = -15
			}
			add_power_balance_value = { # Reverse balance of power effect
				id = GER_balance_of_power
				value = -0.05
			}
		}
	}

	GER_bop_form_division = {
		visible = { # Check that Elite Forces is enabled in game
			has_global_flag = EF_Installed
		}

		available = {
			has_global_flag = EF_Installed
			always = no # Decision is not meant to be taken
		}

		complete_effect = {
			custom_effect_tooltip = GER_bop_form_division_tt # Explain to the player the bop effects of forming elite units
		}
	}

	GER_bop_himmler_inner_circle = {
		visible = {
			always = yes
		}

		available = {
			always = no # Decision is not meant to be taken
		}

		complete_effect = {
			custom_effect_tooltip = GER_bop_himmler_inner_circle_tt # Explain to the player the bop effects of putting Himmler in the inner circle
		}
	}

	GER_bop_goring_inner_circle = {
		visible = {
			always = yes
		}

		available = {
			always = no # Decision not meant to be taken
		}

		complete_effect = {
			custom_effect_tooltip = GER_bop_goring_inner_circle_tt # Explain to the player the bop effects of putting Goring in the inner circle
		}
	}

	GER_bop_promote_general = {
		visible = {
			always = yes
		}

		available = {
			always = no # Decision not meant to be taken
		}

		complete_effect = {
			custom_effect_tooltip = GER_bop_promote_general_tt # Explain to the player the bop effects of promoting generals and field commanders
		}
	}
}